card = []
tmp = []
time = 0

for time in range(10):
    card = card + [eval(input())]
print(card)

for time in range(10):
    tmpnum = 0
    num = []
    if card[time] == card[time1]:
        if num[time] == num[time1]:
            tmpnum += 1
        elif num[]
    print(num[0])
    print(len(num))

